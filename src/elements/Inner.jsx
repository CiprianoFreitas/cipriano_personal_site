import tw from 'tailwind.macro';
import styled from 'styled-components';

const Inner = styled.div`
    ${tw`text-white relative py-16 sm:px-6 sm:py-6 font-sans max-w-xl mx-auto`};
`;

export default Inner;
